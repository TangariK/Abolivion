{
  "name": "abolivion",
  "private": true,
  "version": "0.1.4",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "pack:gamejolt": "npm run build && node scripts/pack-gamejolt.mjs",
    "db:migrate": "node scripts/migrate-supabase.mjs",
    "functions:deploy": "node scripts/deploy-attach-email.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.22.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
